import { registry } from './CapabilityRegistry'

// Register stubs for capabilities (will be fully implemented later)
// For now, the registry is empty but ready for registration

export { registry }
export { BaseCapability, createPipeData } from './BaseCapability'
